[package]
name = "node-bindgen-unitest-error-reproduction"
version = "0.1.0"
edition = "2018"

[dependencies]
node-bindgen = { version = "4.0" }

[build-dependencies]
node-bindgen = { version = "4.0", features = ["build"] }

[lib]
crate-type = ["cdylib"]
